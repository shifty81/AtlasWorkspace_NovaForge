#include "NF/Input/Input.h"

namespace NF {
// Input module — keyboard, mouse, gamepad, action mappings.
// All implementation is header-only via inline definitions.
// This translation unit ensures the static library has at least one object file.
} // namespace NF
